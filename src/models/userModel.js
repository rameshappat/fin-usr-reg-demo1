const db = require('../config/db');\n\nexports.createUser = async ({ email, password, phone, mfaEnabled, kycVerified }) => {\n  const query = `INSERT INTO users (email, password_hash, phone_number, mfa_enabled, kyc_verified) VALUES ($1, $2, $3, $4, $5) RETURNING *`;\n  const values = [email, password, phone, mfaEnabled, kycVerified];\n  const { rows } = await db.query(query, values);\n  return rows[0];\n};